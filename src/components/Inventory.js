import React from 'react';
import InventoryList from './InventoryList';

const Inventory = () => {
    return (
        <div>
            <h1 className="mt-3 text-center">Inventory List</h1>
            <InventoryList />
        </div>
    );
};

export default Inventory;
